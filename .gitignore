# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# Virtual env
.venv/
.venv-codex/
venv/
env/

# ML / experiments
mlruns/
mlartifacts/
mlflow.db
wandb/
*.pkl
*.joblib

# Кэши локальных эмбеддингов: пересчитываются из данных за пару минут,
# держать мегабайты бинарников в истории смысла нет.
data/processed/name_embeddings.npz
data/processed/classifier_embeddings.npz

# Secrets
.env
*.key

# Runtime
gradio.log

# OS / IDE
.DS_Store
.vscode/
.idea/

# Keep data folder structure but ignore large generated files if needed
# (датасет оставляем в репозитории — он небольшой и нужен для воспроизводимости)

# Логи локального демо-API
api.log
